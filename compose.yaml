services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: dari_docs
      POSTGRES_PASSWORD: dari_docs
      POSTGRES_DB: dari_docs_local
    ports:
      - "5432:5432"
    volumes:
      - dari_docs_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dari_docs -d dari_docs_local"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  dari_docs_postgres:
